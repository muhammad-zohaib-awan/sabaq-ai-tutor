{
  "name": "@sabaq/engine",
  "version": "1.0.0",
  "private": true,
  "description": "Framework-agnostic learning experience engine: content -> journey, mastery inference, adaptation, gamification.",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "watch": "tsc -p tsconfig.json --watch"
  },
  "devDependencies": {
    "@types/node": "^22.9.0",
    "typescript": "^5.6.3"
  }
}
